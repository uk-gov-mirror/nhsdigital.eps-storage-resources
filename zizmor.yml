rules:
  unpinned-images:
    # these workflows use unpinned images because they are using a full image passed in that contains the tag
    ignore:
      - cdk_package_code.yml:16:18
      - cdk_release_code.yml:34:18
      - delete_old_cloudformation_stacks.yml:24:18
