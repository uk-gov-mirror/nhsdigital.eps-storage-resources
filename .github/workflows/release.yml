name: Release
on:
  workflow_dispatch:

permissions: {}

jobs:
  get_config_values:
    uses: NHSDigital/eps-common-workflows/.github/workflows/get-repo-config.yml@c4efd04c5ff1c7e92e7fe5932b29d0de1a301cdf # v6.0.14
    with:
      verify_published_from_main_image: false
    permissions:
      attestations: read
      contents: read
      packages: read

  quality_checks:
    uses: NHSDigital/eps-common-workflows/.github/workflows/quality-checks-devcontainer.yml@c4efd04c5ff1c7e92e7fe5932b29d0de1a301cdf # v6.0.14
    needs:
      - get_config_values
    with:
      pinned_image: ${{ needs.get_config_values.outputs.pinned_image }}
    permissions:
      contents: read
      id-token: write
      packages: read
    secrets: 
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  get_commit_id:
    runs-on: ubuntu-22.04
    outputs:
      commit_id: ${{ steps.commit_id.outputs.commit_id }}
    steps:
      - name: Get Commit ID
        id: commit_id
        run: |
          echo "commit_id=${{ github.sha }}" >> "$GITHUB_OUTPUT"

  tag_release:
    needs: [quality_checks, get_commit_id, get_config_values]
    uses: NHSDigital/eps-common-workflows/.github/workflows/tag-release-devcontainer.yml@c4efd04c5ff1c7e92e7fe5932b29d0de1a301cdf # v6.0.14
    permissions:
      contents: write
      id-token: write
      packages: write
    with:
      dry_run: false
      pinned_image: ${{ needs.get_config_values.outputs.pinned_image }}
      branch_name: main
      tag_format: ${{ needs.get_config_values.outputs.tag_format }}

  package_cdk_code:
    needs: [get_config_values]
    uses: ./.github/workflows/cdk_package_code.yml
    permissions:
      contents: read
      packages: read
      id-token: write
    with:
      pinned_image: ${{ needs.get_config_values.outputs.pinned_image }}

  deploy_dev:
    needs: [quality_checks, package_cdk_code, tag_release, get_commit_id, get_config_values]
    uses: ./.github/workflows/cdk_release_code.yml
    permissions:
      contents: write
      id-token: write
    with:
      pinned_image: ${{ needs.get_config_values.outputs.pinned_image }}
      stack_name: data-extract
      target_environment: dev
      version: ${{ needs.tag_release.outputs.version_tag }}
      commit_id: ${{ needs.get_commit_id.outputs.commit_id }}
      is_pull_request: false
    secrets:
      CLOUD_FORMATION_DEPLOY_ROLE: ${{ secrets.DEV_CLOUD_FORMATION_DEPLOY_ROLE }}

  deploy_qa:
    needs: [get_config_values, get_commit_id, tag_release, deploy_dev]
    uses: ./.github/workflows/cdk_release_code.yml
    permissions:
      contents: write
      id-token: write
    with:
      pinned_image: ${{ needs.get_config_values.outputs.pinned_image }}
      stack_name: data-extract
      target_environment: qa
      version: ${{ needs.tag_release.outputs.version_tag }}
      commit_id: ${{ needs.get_commit_id.outputs.commit_id }}
      is_pull_request: false
    secrets:
      CLOUD_FORMATION_DEPLOY_ROLE: ${{ secrets.QA_CLOUD_FORMATION_DEPLOY_ROLE }}

  deploy_int:
    needs: [get_config_values, get_commit_id, tag_release, deploy_dev]
    uses: ./.github/workflows/cdk_release_code.yml
    permissions:
      contents: write
      id-token: write
    with:
      pinned_image: ${{ needs.get_config_values.outputs.pinned_image }}
      stack_name: data-extract
      target_environment: int
      version: ${{ needs.tag_release.outputs.version_tag }}
      commit_id: ${{ needs.get_commit_id.outputs.commit_id }}
      is_pull_request: false
    secrets:
      CLOUD_FORMATION_DEPLOY_ROLE: ${{ secrets.INT_CLOUD_FORMATION_DEPLOY_ROLE }}

  deploy_ref:
    needs: [get_config_values, get_commit_id, tag_release, deploy_dev]
    uses: ./.github/workflows/cdk_release_code.yml
    permissions:
      contents: write
      id-token: write
    with:
      pinned_image: ${{ needs.get_config_values.outputs.pinned_image }}
      stack_name: data-extract
      target_environment: ref
      version: ${{ needs.tag_release.outputs.version_tag }}
      commit_id: ${{ needs.get_commit_id.outputs.commit_id }}
      is_pull_request: false
    secrets:
      CLOUD_FORMATION_DEPLOY_ROLE: ${{ secrets.REF_CLOUD_FORMATION_DEPLOY_ROLE }}


  deploy_prod:
    needs: [get_config_values, get_commit_id, tag_release, deploy_int]
    uses: ./.github/workflows/cdk_release_code.yml
    permissions:
      contents: write
      id-token: write
    with:
      pinned_image: ${{ needs.get_config_values.outputs.pinned_image }}
      stack_name: data-extract
      target_environment: prod
      version: ${{ needs.tag_release.outputs.version_tag }}
      commit_id: ${{ needs.get_commit_id.outputs.commit_id }}
      is_pull_request: false
    secrets:
      CLOUD_FORMATION_DEPLOY_ROLE: ${{ secrets.PROD_CLOUD_FORMATION_DEPLOY_ROLE }}
